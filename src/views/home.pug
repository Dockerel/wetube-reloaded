extends base
include mixins/video


block content
    div.video_grid
        each video in videos
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            
        else
            li Sorry nothing found.